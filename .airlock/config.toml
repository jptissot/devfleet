# generated by `airlock init` — shared; safe to commit
image = "airlock-devfleet-bc207196e39b:latest"
cmd   = ["claude"]

[init]                       # init's record; the launcher ignores this
tools     = []
harnesses = ["claude-code"]
default   = "claude-code"

# generated by `airlock init`
# Local-only by default — the whole directory is ignored. To share this
# project's toolchain and launch settings with the repo, uncomment
# .gitignore AND the file(s) you want tracked. config.local.toml and env
# hold host paths and secrets — leave them out.
*
!.gitignore
!Containerfile
!config.toml
